#include "algorithm.h"
/*
static uint partition(void *arr, uint low, uint high, cmp_func cmp)
{
	void *x = &arr[low];

	while (low < high) {
		for (;low < high; high--) {
			if (cmp(x, &arr[high]) > 0) {
				
			}
		}
	}
}

void quiksort(void *arr, uint low, uint high, cmp_func cmp)
{
	std::sort()
}
*/